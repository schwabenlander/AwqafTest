@model IEnumerable<AwqafTest.Models.ViewModels.VoucherViewModel>

@{
    ViewData["Title"] = "Vouchers";
}

<h1>@ViewData["Title"]</h1>

<p>
    <a asp-action="Create">Add New Voucher</a>
</p>
<table class="table">
    <thead>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VoucherId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VoucherDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VoucherStatusId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VoucherTotal)
        </th>
    </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.VoucherId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.VoucherDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.VoucherStatusId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.VoucherTotal)
            </td>
        </tr>
        }
    </tbody>
</table>
